name: workflow

on:
  - push
  - workflow_dispatch

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    container:
      image: alpine:3.22
    steps:
      - name: info machine
        run: hostnamectl
      - name: ip
        run: ip -4 a
      - name: mon identité de connexion
        run: whoami


